import React from 'react'
import {
    FormControl,
    FormLabel,
    FormHelperText,
    Input,
} from "@chakra-ui/react"
    import { Form } from 'react-router-dom'
const profile = () => {
  return (
    <div>profile</div>
  )
}

export default profile;